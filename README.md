# Simple dataset viewer using MongoDB Angular and Django

## Usage
Run this command to start the server:
```bash
docker-compose -f docker-compose.yml -f docker-compose.override.yml up
```

Navigate to http://localhost:4200
